{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ note.note }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ note.dateCreation ? note.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_note_crud_index') }}">back to list</a>

    <a href="{{ path('app_note_crud_edit', {'id': note.id}) }}">edit</a>

    {{ include('note_crud/_delete_form.html.twig') }}
{% endblock %}
